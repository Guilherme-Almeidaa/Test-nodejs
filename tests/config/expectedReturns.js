const userNotFound = {
    error: {
        message: 'user not found',
    },
};

const nicknameNotExists = {
    error: {
        message: 'nickname does not exist',
    },
};

module.exports = {
  userNotFound,
  nicknameNotExists,
}